<?xml version="1.0" encoding="UTF-8"?>
<!-- RCC website autonomous nightly run. Install:
       cp to ~/Library/LaunchAgents/ and
       launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.jason.rcc-web.nightly.plist
     Runs on DD-Mini (primary always-on box) at 03:15 daily — staggered an hour after
     com.jason.cairn.nightly (02:15) so the two big runs don't collide. Injects into the
     JARVIS-rcc-web tmux session; loop state is ui-shell/docs/NIGHTLY-LOOP.md. -->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.jason.rcc-web.nightly</string>
    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>/Users/jason/dev/rcc/ui-shell/scripts/nightly.sh</string>
    </array>
    <key>StartCalendarInterval</key>
    <dict>
        <key>Hour</key>
        <integer>3</integer>
        <key>Minute</key>
        <integer>15</integer>
    </dict>
    <key>StandardOutPath</key>
    <string>/Users/jason/Library/Logs/rcc-web-nightly.out.log</string>
    <key>StandardErrorPath</key>
    <string>/Users/jason/Library/Logs/rcc-web-nightly.err.log</string>
    <key>RunAtLoad</key>
    <false/>
</dict>
</plist>
